<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="3dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:id="@+id/rvControllerListConstraintLayout">
    <LinearLayout
        android:id="@+id/rvControllerListLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:background="@drawable/bg_controller_list_item"
        android:gravity="center"
        android:paddingVertical="10dp"
        android:layout_marginHorizontal="5dp">
        <TextView
            android:id="@+id/rvControllerListTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:textSize="16dp"
            android:textColor="@color/white_cream"
            android:text="Text"/>
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>